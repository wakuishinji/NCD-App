{
  "scripts": {
    "test": "vitest",
    "test:playwright": "playwright test",
    "migrate:master-ids": "node ./scripts/migrateMasterIds.mjs",
    "cleanup:legacy-masters": "node ./scripts/cleanupLegacyMasters.mjs",
    "report:master-orphans": "node ./scripts/reportMasterKvOrphans.mjs",
    "publish:mhlw": "node ./scripts/publishMhlwFacilities.mjs",
    "upload:mhlw": "node ./scripts/uploadMhlwToR2.mjs",
    "backfill:facilities": "node ./scripts/backfillFacilityCollections.mjs",
    "seed:org-masters": "node ./scripts/seedOrganizationMasters.mjs",
    "pages:dev": "wrangler pages dev ./web --local true",
    "deploy:pages:staging": "wrangler pages deploy ./web --project-name ncd-app --branch staging",
    "deploy:pages:production": "wrangler pages deploy ./web --project-name ncd-app --branch main"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.8",
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.9.1",
    "vitest": "^3.2.4",
    "wrangler": "^4.38.0"
  }
}
